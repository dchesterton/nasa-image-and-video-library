import { StyleSheet } from "aphrodite/no-important";

export const styles = StyleSheet.create({
    app: {
        textAlign: "center"
    },
    logo: {
        marginTop: "8vmin",
        marginBottom: "4vmin",
        height: "25vmin",
        minHeight: "160px"
    }
});
